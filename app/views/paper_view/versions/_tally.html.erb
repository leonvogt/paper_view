<span class="pv-tally pv-tally--add">+<%= diff.added_count %></span>
<span class="pv-tally pv-tally--del">−<%= diff.removed_count %></span>
<span class="pv-tally__unit"><%= "line".pluralize(diff.added_count + diff.removed_count) %></span>
